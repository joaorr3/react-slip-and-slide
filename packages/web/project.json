{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/web/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/web",
        "main": "packages/web/src/index.ts",
        "tsConfig": "packages/web/tsconfig.lib.json",
        "assets": ["packages/web/*.md"],
        "buildableProjectDepsInPackageJsonType": "dependencies",
        "excludeDeps": [
          "react-dom",
          "react-native",
          "@react-spring/native",
          "tslib"
        ]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs web {args.ver} {args.tag} {args.otp}"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/web/**/*.ts"]
      }
    }
  },
  "tags": []
}
